[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "createcart-cart"
version = "0.1.0"
description = "CreateCart Cart SDK — headless, pluggable shopping cart with totals, charges, discount and tax."
readme = "README.md"
requires-python = ">=3.10"
authors = [{ name = "CreateCart" }]
license = { text = "Proprietary" }
dependencies = [
    "pydantic>=2.6",
]

[project.optional-dependencies]
dev = [
    "pytest>=8.0",
]

[tool.hatch.build.targets.wheel]
packages = ["src/createcart_cart"]

[tool.pytest.ini_options]
testpaths = ["tests"]
